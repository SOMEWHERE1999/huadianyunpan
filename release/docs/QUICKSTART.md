# 快速开始

1. 安装 WinFsp。
2. 将三个 EXE 放入 bin/，认证信息保存在发行目录之外。
3. 按主 README 的命令启动 daemon。
4. 另开 PowerShell，以相同 pipe 挂载 S:。
5. 在资源管理器浏览、下载或向新路径复制文件。
6. 同名冲突请选择跳过；保留两份请用 AutoRename。
7. 完成后停止 hddfs 和 hddsyncd。

首次运行只使用测试目录，不要在重要远端目录直接移动或删除。
